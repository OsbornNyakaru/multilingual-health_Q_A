# Pinned dependencies for full pipeline (training + eval + inference).
# Python 3.11. Mirror of pyproject.toml [project].dependencies.
# Every version pinned with == per Zindi reproducibility rules.

torch==2.4.1
transformers==4.46.2
accelerate==1.1.1
peft==0.13.2
bitsandbytes==0.44.1
datasets==3.1.0
trl==0.12.1
sentencepiece==0.2.0
protobuf==5.28.3
rouge-score==0.1.2
sacrebleu==2.4.3
fasttext==0.9.3
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.5.2
wandb==0.18.7
pyyaml==6.0.2
tqdm==4.67.0
hydra-core==1.3.2
pytest==8.3.3
ruff==0.7.4

# Transitive pins worth locking explicitly to avoid drift.
tokenizers==0.20.3
safetensors==0.4.5
huggingface-hub==0.26.2
